'use client'

import { TextInput } from '@payloadcms/ui'

/**
 * Placeholder — replaced by Task 5 implementation.
 * Exists now so the import map resolves and the admin panel compiles.
 */
export function SmtpApiKeyField() {
  return <TextInput path="apiKey" label="API Key" />
}
